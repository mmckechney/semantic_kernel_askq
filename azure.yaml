name: document-questions
metadata:
  template: custom


infra:
  provider: bicep
  path: ./infra
  module: main

hooks:
  preup: 
    - shell: pwsh
      run: ./scripts/preup.ps1

  postdeploy:
    - shell: pwsh
      run: ./scripts/postdeploy.ps1